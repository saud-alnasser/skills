# AEP — the repository that builds it

Read **`AGENTS.md`** in this directory. It is the entrypoint, and it is not
Claude-specific.

It points at `.aep/protocol.md`, which is where the protocol actually starts.

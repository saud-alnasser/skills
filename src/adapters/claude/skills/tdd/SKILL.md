---
name: tdd
description: Test-driven development. Use when building a behaviour test-first, or pinning a bug down before fixing it.
metadata:
  aep: adapter
  canonical: .aep/skills/tdd.md
---

Read `.aep/skills/tdd.md` and follow it exactly. That file is the skill; this one only routes to it.

If `.aep/skills/tdd.md` does not exist, this repository has not installed AEP.
For `/aep:install` and `/aep:help`, fall back to
`${CLAUDE_PLUGIN_ROOT}/src/skills/tdd.md` and continue.
For anything else, say AEP is not installed here and offer `/aep:install` —
do not improvise the skill.

---
owner: framework
type: norm
subject: knowledge
fires-when: stage
stages: [design]
spans:
  - design-writes-as-it-resolves: ruw0ro
  - a-decision-is-offered-only-at-the-bar: b9dcvc
---


# Writing knowledge

Which stage may write which knowledge layer. The layers themselves, and the truth hierarchy over them, are in `CLAUDE.md`; this is who holds the pen.

## Design writes as it resolves

- **`/design` writes vocabulary and Decisions as they resolve, never batched at the end** — the grill produces them, and batching loses the half that felt obvious at the time.

## A Decision is offered only at the bar

- **A Decision is offered only when it clears the bar in `decisions.md`** — most things do not.
